# Placeholder — replace by running:  sops secrets/secrets.yaml
# (sops encrypts this file in place using the keys from .sops.yaml)
anthropic_auth_token: REPLACE_ME
ssh_key: |
  REPLACE_ME

# Rendered into the stepping-stone shared env file (modules/services/
# stepping-stone). Values marked CHANGE-ME must be filled in before first
# boot — see MIGRATION.md.
stepping-stone:
  db_password: CHANGE-ME
  vikunja_service_secret: CHANGE-ME
  kopia_repo_password: CHANGE-ME
